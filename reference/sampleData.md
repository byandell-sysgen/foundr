# Sample Data for Package foundr

A data frame with columns \`dataset\`, \`strain\`, \`sex\`, \`animal\`,
\`condition\`, \`trait\`, \`value\`

## Details

\`sampleData\` is a minimal data example for the foundr package.

## Examples

``` r
data(sampleData)
```
