# Calculate area under curve over time

Calculate area under curve over time

## Usage

``` r
area_under_curve(object, timecol = c("minutes", "minute", "week"))
```

## Arguments

- object:

  data frame with \`value\` and other columns

- timecol:

  column containing time values

## Value

object collapsed over time with summaries
