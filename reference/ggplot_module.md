# GGplot of Module P-values

GGplot of Module P-values

## Usage

``` r
ggplot_module(object, terms = c("p_strain_sex", "p_strain"))
```

## Arguments

- object:

  data frame with appropriate columns

- terms:

  names of two columns with p-values

## Value

ggplot2 object
